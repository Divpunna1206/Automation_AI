# Frontend

Minimal Next.js dashboard for Phase 1.

## Run

```powershell
cd frontend
npm.cmd install
npm.cmd run dev
```

Set `NEXT_PUBLIC_API_BASE_URL` if the backend is not on `http://localhost:8000`.
